# The Judge - Project Rules

## Changelog Rule

Every change to this project MUST be recorded in `CHANGELOG.md` at the project root before the change is considered complete. Entries go under the current version section with the appropriate category (`Added`, `Changed`, `Fixed`, `Removed`). If a new version section doesn't exist yet, create one. Never modify entries for already-released versions.
